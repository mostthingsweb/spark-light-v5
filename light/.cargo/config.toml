[target.xtensa-esp32s3-none-elf]
runner = "espflash flash --monitor --chip esp32s3 --no-stub"

[env]

[build]
rustflags = [
  "-C", "link-arg=-nostartfiles",
]

target = "xtensa-esp32s3-none-elf"

[unstable]
build-std = ["alloc", "core"]
